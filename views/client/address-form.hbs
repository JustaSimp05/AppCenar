<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-lg-8 col-md-10">
      <div class="card shadow-sm">
        <div class="card-header d-flex justify-content-between align-items-center">
          <h4 class="mb-0">
            {{#if address._id}}
              <i class="fas fa-edit me-2"></i> Editar dirección
            {{else}}
              <i class="fas fa-map-marker-alt me-2"></i> Nueva dirección
            {{/if}}
          </h4>
          <span class="badge bg-light text-muted">
            {{user.nombre}} {{user.apellido}}
          </span>
        </div>

        <div class="card-body">
          {{#if error_msg}}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
              {{error_msg}}
              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
          {{/if}}

          <form action="{{formAction}}" method="POST" novalidate>
            <div class="mb-3">
              <label for="nombre" class="form-label">
                Nombre de la dirección
                <span class="text-danger">*</span>
              </label>
              <input
                type="text"
                id="nombre"
                name="nombre"
                class="form-control"
                placeholder="Ej: Casa, Oficina, Apartamento mamá"
                value="{{address.nombre}}"
                required
              >
              <div class="form-text">
                Usa un nombre corto para identificarla fácilmente.
              </div>
            </div>

            <div class="mb-3">
              <label for="descripcion" class="form-label">
                Descripción
                <span class="text-danger">*</span>
              </label>
              <textarea
                id="descripcion"
                name="descripcion"
                class="form-control"
                rows="3"
                placeholder="Ej: Calle 10 #23, edificio azul, apto 3B, referencia: frente al colmado La Esquina"
                required
              >{{address.descripcion}}</textarea>
              <div class="form-text">
                Describe la dirección lo más claro posible para el delivery.
              </div>
            </div>

            <div class="d-flex justify-content-between mt-4">
              <a href="/client/addresses" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-1"></i> Volver al listado
              </a>
              <button type="submit" class="btn btn-primary">
                <i class="fas fa-save me-1"></i> {{submitText}}
              </button>
            </div>
          </form>
        </div>

        <div class="card-footer text-muted small">
          Todos los campos marcados con <span class="text-danger">*</span> son obligatorios.
        </div>
      </div>
    </div>
  </div>
</div>
