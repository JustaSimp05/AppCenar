<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      
      <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
        
        <div class="card-header bg-white border-bottom p-4">
          <div class="d-flex justify-content-between align-items-center">
            <h4 class="mb-0 fw-bold text-dark">
              {{#if address._id}}
                <i class="fas fa-edit text-primary me-2"></i>Editar Dirección
              {{else}}
                <i class="fas fa-map-marker-alt text-primary me-2"></i>Nueva Dirección
              {{/if}}
            </h4>
            <span class="badge bg-primary bg-opacity-10 text-primary rounded-pill px-3 py-2">
              <i class="fas fa-user me-1"></i> {{user.nombre}}
            </span>
          </div>
        </div>

        <div class="card-body p-4 p-md-5">
          
          {{!-- Mensajes de Error --}}
          {{#if error_msg}}
            <div class="alert alert-danger alert-dismissible fade show shadow-sm border-0 mb-4" role="alert">
              <i class="fas fa-exclamation-circle me-2"></i> {{error_msg}}
              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
          {{/if}}

          <form action="{{formAction}}" method="POST" novalidate>
            
            <div class="mb-4">
              <label for="nombre" class="form-label small fw-bold text-muted text-uppercase">
                Nombre de la dirección <span class="text-danger">*</span>
              </label>
              <div class="input-group">
                <span class="input-group-text bg-light border-end-0 text-muted"><i class="fas fa-tag"></i></span>
                <input
                  type="text"
                  id="nombre"
                  name="nombre"
                  class="form-control bg-light border-start-0"
                  placeholder="Ej: Casa, Oficina"
                  value="{{address.nombre}}"
                  required
                >
              </div>
              <div class="form-text text-muted small mt-1">
                Un nombre corto para identificarla rápido.
              </div>
            </div>

            <div class="mb-4">
              <label for="descripcion" class="form-label small fw-bold text-muted text-uppercase">
                Descripción detallada <span class="text-danger">*</span>
              </label>
              <textarea
                id="descripcion"
                name="descripcion"
                class="form-control bg-light"
                rows="4"
                placeholder="Calle, número, edificio, apartamento y referencias..."
                required
              >{{address.descripcion}}</textarea>
              <div class="form-text text-muted small mt-1">
                Ayuda al delivery a llegar más rápido (ej: "Frente al parque").
              </div>
            </div>

            <div class="d-grid gap-3 pt-2">
              <button type="submit" class="btn btn-primary btn-lg fw-bold shadow-sm">
                <i class="fas fa-save me-2"></i> {{submitText}}
              </button>
              
              <a href="/client/addresses" class="btn btn-light text-muted">
                Cancelar y volver
              </a>
            </div>

          </form>
        </div>
      </div>

    </div>
  </div>
</div>