<div class="mt-4 row justify-content-center">
  <div class="col-md-8">
    <div class="card shadow">
      <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
        <h4 class="mb-0">Configuración del Sistema</h4>
        <i class="fas fa-sliders-h"></i>
      </div>
      <div class="card-body">
        <p class="text-muted">Estos valores afectarán a todos los pedidos futuros de la aplicación.</p>
        
        <form action="/admin/config" method="POST">
          
          <div class="mb-4">
            <h5 class="text-primary border-bottom pb-2">
              <i class="fas fa-file-invoice-dollar me-2"></i>Datos Fiscales
            </h5>
            <div class="mb-3">
              <label class="form-label fw-bold">ITBIS General (%)</label>
              <div class="input-group">
                <input type="number" step="0.01" name="itbis" class="form-control" value="{{config.itbis}}" required>
                <span class="input-group-text">%</span>
              </div>
              <div class="form-text">Porcentaje aplicado al subtotal de cada orden.</div>
            </div>
          </div>

          <div class="mb-4">
            <h5 class="text-primary border-bottom pb-2">
              <i class="fas fa-motorcycle me-2"></i>Logística y Delivery
            </h5>
            <div class="row">
              <div class="col-md-6 mb-3">
                <label class="form-label fw-bold">Costo de Envío Base</label>
                <div class="input-group">
                  <span class="input-group-text">RD$</span>
                  <input type="number" step="0.01" name="costoEntrega" class="form-control" value="{{config.costoEntrega}}" required>
                </div>
              </div>
              <div class="col-md-6 mb-3">
                <label class="form-label fw-bold">Tiempo Promedio Entrega</label>
                <div class="input-group">
                  <input type="number" name="tiempoEntrega" class="form-control" value="{{config.tiempoEntrega}}" required>
                  <span class="input-group-text">minutos</span>
                </div>
              </div>
            </div>
          </div>

          <div class="d-flex justify-content-between align-items-center mt-4">
            <a href="/admin/home" class="btn btn-outline-secondary px-4">Cancelar</a>
            <button type="submit" class="btn btn-primary btn-lg px-5">
              <i class="fas fa-save me-2"></i>Guardar Cambios
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>