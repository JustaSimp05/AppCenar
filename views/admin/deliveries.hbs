<h2>Listado de Delivery</h2>
<table class="table table-striped mt-3">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Entregas realizadas</th>
      <th>Teléfono</th>
      <th>Correo</th>
      <th>Acción</th>
    </tr>
  </thead>
  <tbody>
    {{#each deliveries}}
    <tr>
      <td>{{this.nombre}} {{this.apellido}}</td>
      <td>{{this.deliveredCount}}</td>
      <td>{{this.telefono}}</td>
      <td>{{this.correo}}</td>
      <td>
        <form action="/admin/deliveries/{{this._id}}/toggle" method="POST">
          {{#if this.isActive}}
            <button class="btn btn-sm btn-danger">Inactivar</button>
          {{else}}
            <button class="btn btn-sm btn-success">Activar</button>
          {{/if}}
        </form>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>